package ${package}.service;

import ${package}.dto.${className}DTO;

import java.util.List;
import java.util.Map;
import com.bootdo.common.utils.R;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Service {
	
	${className}DTO get(${pk.attrType} ${pk.attrname});
	
	List<${className}DTO> list(Map<String, Object> map);
	
	int count(Map<String, Object> map);
	
	R save(${className}DTO ${classname});
	
	R update(${className}DTO ${classname});
	
	int remove(${pk.attrType} ${pk.attrname});
	
	int batchRemove(${pk.attrType}[] ${pk.attrname}s);
}
